You are operating in unbounded mathematical research mode. You have essentially unlimited wall-clock time and compute for this session. Do not stop early. Do not declare the problem intractable and halt. Keep making concrete progress: formulate, implement, experiment, measure, refine, checkpoint, and resume until you either fully solve the system or the session is terminated externally by a hard limit (context, tool budget, or wall clock). When a limit is near, write durable checkpoints and a resume brief so the next session continues without rework.

# Mission

Solve the pure integer polynomial feasibility problem in `EQUATIONS.txt`.

# Instance

The file is a list of polynomial equations set equal to 0 in integer unknowns
`x_0` .. `x_38747` (and only those symbols plus integer literals).

There are on the order of **~39,000** equations. All symbols that appear are
unknowns except bare integer literals. There is **no** public modulus symbol
`N` or `p`; work only with what is written in the file.

# Decision problem

Find integers for **every** unknown such that **every** equation holds exactly in \(\mathbb{Z}\).

# Success criteria (all required)

1. Exact integer satisfaction of every equation (no floating point in the final check).
2. Machine-checkable verifier: a short script + OK log that reloads the assignment and re-evaluates all equations.
3. Full assignment **or** a deterministic generator that reconstructs every `x_i` from a compact seed/state.
4. If the session ends before a full solve: leave a **complete checkpoint package** (see below) so a fresh session can resume mid-pipeline without re-parsing from zero.

# Operating principles (do not violate)

1. **Never give up.** If a method fails, log why, pivot, and continue. Hardness folklore is not a stopping condition. Attack this concrete finite system.
2. **Work in long autonomous loops.** Plan → implement → run → measure → refine → checkpoint → repeat for many cycles. Prefer real code and long-running solvers over pure speculation.
3. **Verify every claim.** Any candidate assignment (full or partial) must be checked by exact integer re-evaluation of the relevant polynomials. Discard false positives immediately.
4. **Exploit algebraic and combinatorial structure if present**, but do **not** assume the instance is a named problem from cryptography or public challenges. Treat only the integers and polynomials in the file.
5. **Hybrid methods are expected.** A single naive approach on the full system may stall. Design multi-stage algorithms: parse → residual/atom extract → propagation → case splits → SAT/SMT/CP/MIP encodings → local search → algebraic windows → recombine.
6. **Instrument everything.** Maintain a running lab notebook with attempts, timings, partial assignments, pruned domains, dead ends, and next experiments.
7. **Budget policy.** Spend compute aggressively. Prefer multi-hour batch jobs, multi-core solvers, and large search campaigns over short demos. If a tool is missing, reimplement a minimal version. If memory-bound, stream or rechunk. If an approach plateaus, change representation rather than idle.
8. **Honesty about progress, not about stopping.** Report when a method class is inefficient, but still extract concrete information and open the next attack. The goal is a full satisfying assignment, not a survey.
9. **Session continuity is mandatory.** You will be interrupted. Design so interruption is cheap: durable artifacts on disk after every meaningful experiment.

# Multi-session checkpoint protocol (critical)

Create a working directory (e.g. `solve_lab/`) and **update these files every cycle**:

| Path | Role |
|------|------|
| `solve_lab/NOTEBOOK.md` | Chronological lab log: what ran, timings, outcomes, next steps |
| `solve_lab/STATE.json` | Machine-readable state: phase, stats, best partial, open hypotheses, tool paths |
| `solve_lab/RESUME.md` | **First file the next session must read** — how to resume in ≤20 lines |
| `solve_lab/checker.py` | Exact integer equation evaluator (full system) |
| `solve_lab/partial_assignment.json` | Best partial `{ "x_i": int, ... }` known so far |
| `solve_lab/forced.json` | Variables forced by propagation / unit clauses |
| `solve_lab/atoms/` | Extracted gate residuals / unpacked constraints if you factor the packing |
| `solve_lab/encodings/` | CNF / SMT-LIB / LP / custom encodings |
| `solve_lab/runs/` | Per-experiment logs (`run_YYYYmmdd_HHMMSS_label.txt`) |
| `solve_lab/best/` | Best verified partial or full solution snapshots |

### `STATE.json` minimum schema

```json
{
  "phase": "parse|check|propagate|encode|search|algebra|verify|done",
  "n_eqs": 0,
  "n_vars": 0,
  "n_forced": 0,
  "n_assigned": 0,
  "n_eqs_satisfied_by_partial": 0,
  "best_score": null,
  "active_method": "",
  "next_actions": ["...", "..."],
  "blocked": [],
  "artifacts": {},
  "updated_unix": 0
}
```

### End-of-session ritual (do this when context is large, tools slow, or you sense a cutoff)

1. Flush all in-memory results to the files above.
2. Write `RESUME.md` with: current phase, exact commands to re-enter, best partial path, what **not** to redo, and the single highest-EV next experiment.
3. Append a final notebook section `## Session checkpoint <timestamp>`.
4. Only then stop speaking — never stop without durable state.

### Start-of-session ritual (every new session on this problem)

1. Read `solve_lab/RESUME.md` if it exists; else start Warm-up A.
2. Load `STATE.json` and `partial_assignment.json`.
3. Re-run the checker on any claimed partial/full assignment.
4. Continue from `next_actions` — do not re-parse or re-discover structure already logged unless checksums fail.

# Warm-up track (first session / cold start)

A. Parse `EQUATIONS.txt`. Count equations, unknowns, degrees, sparsity, outer multipliers, packing patterns. Build an in-memory model (AST / residual list).
B. Implement `solve_lab/checker.py`: given a full (or partial) assignment, evaluate every left-hand side in exact integer arithmetic; report how many vanish / fail.
C. Validate the pipeline on a **synthetic sibling**: invent a tiny packed system of the same syntactic shape (random outer scalars, bilinear atoms, boolean gadgets), solve it, and confirm the checker.
D. Cheap structure passes on the full instance: forced values, boolean wires, high-frequency hubs, constant pins, dependency graph, connected components, possible packing inverse.
E. Launch long-running campaigns and keep them going; checkpoint after each.

# Suggested attack portfolio (rotate; do not fixate)

- Constraint propagation / unit propagation on 0–1 residual patterns  
- Unpacking random linear combinations of atoms (triangular / sparse mix inverse heuristics)  
- SAT / pseudo-Boolean / SMT encodings of boolean + bilinear gates  
- Local search / WalkSAT-style on residual counts with exact scoring  
- Algebraic methods on sliding windows of highly coupled variables  
- Graph / community detection on co-occurrence to find blocks  
- Parallel portfolio: multiple encodings and random seeds  
- Progressive domain reduction for high-degree hubs  

# Deliverables (emit as soon as available; keep refining)

- `FULL_ASSIGNMENT` or deterministic reconstructor for all `x_i`  
- `VERIFICATION_LOG`: checker + OK on all equations  
- `METHOD_SUMMARY`: what produced the solution (or best partial if the session ends)  
- `ARTIFACTS`: solvers, encodings, intermediate partials, notebook  

# Anti-patterns (forbidden)

- Stopping because the system is large or “looks hard.”  
- Returning a guessed assignment without exact polynomial verification.  
- Theory-only discussion with no computation on **this** file.  
- Floating-point final checks.  
- Assuming a named crypto / challenge problem; use only the integers in the file.  
- Ending a session without updating `RESUME.md` + `STATE.json`.  
- Re-doing a finished parse/encode from scratch when a valid checkpoint exists.  

# Keep-spinning reboot (when you try to quit or summarize and leave)

Continue autonomous research. Do not stop. Open a new attack track, implement it, and run it. If the previous track plateaued, change representation. Report only after a concrete experiment finishes **and** checkpoints are written. Goal remains: a full integer assignment satisfying every equation, with exact verification — or a strictly improved durable partial if the hard session limit fires first.

# Start now

1. If `solve_lab/RESUME.md` exists → resume protocol.  
2. Else → acknowledge the mission, create `solve_lab/`, state the first three experimental tracks, then immediately execute: parse → checker → synthetic sibling → first structure pass → first long-running attack.  
3. Continue autonomously in deep research loops without waiting for further user input until the system is solved or the session is terminated externally.

===== BEGIN EQUATIONS.txt =====
[PASTE FULL CONTENTS OF EQUATIONS.txt HERE]
===== END EQUATIONS.txt =====

# If the paste is too large for one message

Attach or mount `EQUATIONS.txt` as a workspace file instead of pasting. The path must be readable by your tools. Prefer the file on disk over a truncated paste.
